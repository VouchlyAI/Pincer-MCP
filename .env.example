# Vault Configuration
VAULT_DB_PATH=~/.pincer/vault.db
AUDIT_LOG_PATH=~/.pincer/audit.jsonl

# Master Key Storage: NEVER STORE HERE!
# The master encryption key is stored in the OS-native keychain:
# - macOS: Keychain Access (service: "pincer-mcp", account: "master-key")
# - Windows: Credential Manager
# - Linux: GNOME Keyring / KWallet
#
# Initialize with: npm run vault:init
